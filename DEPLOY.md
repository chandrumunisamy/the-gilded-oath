# Publish update

Copy these files into the existing `the-gilded-oath` repository folder without deleting its hidden `.git` folder, then run:

```powershell
git add -A
git commit -m "Fix image sizing and mobile performance"
git push origin main
```

GitHub Pages will rebuild automatically.
